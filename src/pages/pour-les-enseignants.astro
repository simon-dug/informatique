---
/**
 * Page « Pour les enseignants ».
 * Son texte se modifie dans src/content/infos/pour-les-enseignants.md
 */
import { render } from "astro:content";
import Base from "../layouts/Base.astro";
import BarreOutils from "../components/BarreOutils.astro";
import { pageInfo } from "../lib/contenu";

const page = await pageInfo("pour-les-enseignants");
const { Content } = await render(page);
---

<Base
  titre={page.data.titre}
  intro={page.data.description}
  description={page.data.description}
>
  <div class="section page">
    <BarreOutils />
    <article class="contenu">
      <Content />
    </article>
  </div>
</Base>
